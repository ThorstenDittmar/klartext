# Improvement Step — <your endeavour> enactment

> **L2 worked example (method seed).** `EXAMPLE — replace with yours.` This shows the skeleton filled, so the
> shape is concrete. The content below is a *model* (drawn from the source endeavour, war-stories stripped) —
> re-decide every binding for your own endeavour, then delete this banner.
> **Out of scope.** The generic definition (the L3 card). Other practices; decisions/rationale.
> **Language.** <your documentation language>.
>
> **L3 definition:** [`../../library/practices/improvement-step.md`](../../library/practices/improvement-step.md)
> **Status:** living (ritual) · **Owner:** the method/organisation role · **Advances Alpha:** Way of Working · **NN:** ✓

## Your bindings

| Generic step (from L3) | Example binding (replace) |
|---|---|
| **Classify** (knowledge-base first) | Look the element type/Alpha up in your own meta-language reference (`<your-semat-definition>` + glossary) *before* solutioning; web only on a miss. |
| **Record** | Durable home is `<your-decisions-doc>` (decision + rationale); new/updated terms go to `<your-glossary>`. |
| **Route** | Via the knowledge-routing practice — **the user is the channel**; no role writes into another role's files. Briefings are routed (or an explicit "none affected"). |
| **Set next step** | A pointer to the next open topic, so a context reset cannot sever the loop. |

## Enforcement (here)

Ritual today. Promotion path: encode the classify+record loop as a skill/hook (closer to mechanical) — the
direction is always ritual → mechanized.

## Evidence

`<link to your improvement register / the runs where this was applied>`.
