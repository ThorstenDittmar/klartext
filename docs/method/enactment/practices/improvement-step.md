# Improvement Step — klartext enactment

> **Scope.** klartext's enactment of the **Improvement Step** practice: the durable homes, the channel, and
> the evidence of our runs.
> **Out of scope.** The generic definition — see the L3 card. Other practices' cards; method decisions and
> rationale (`../continuous-improvement.md`).
> **Anti-pattern guarded.** Advisory-only process knowledge that lives in chat and gets lost (see
> `../continuous-improvement.md` §0 / RC2).
> **Language.** English — documentation-language rule.
>
> **L3 definition:** [`../../library/practices/improvement-step.md`](../../library/practices/improvement-step.md)
> **Status:** living (ritual) · **Owner:** OE · **Advances Alpha:** Way of Working · **NN:** ✓

## klartext bindings

| Generic step | klartext binding |
|---|---|
| **Classify** (KB-first) | `semat-definition.md` + `semat-glossary.md` first, web only on a miss. The element type/Alpha is located in our own reference before solutioning (Method Keeper pattern, OE). |
| **Record** | Durable home is `../continuous-improvement.md` (decision + rationale); new/updated terms go to `semat-glossary.md`. |
| **Route** | Via the `knowledge-routing` skill — **the user is the channel**; no agent writes directly into another agent's files. Briefings routed (or an explicit "none affected"). |
| **Set next step** | A pointer to the next open topic so a `/compact` cannot sever the loop. |

## Enforcement (klartext)

Currently a **ritual** (Enforcement Hierarchy level 2). Promotion to a **Skill** (closer to mechanical) is
**deferred until after the first real run** validates the shape ("instantiate through use" — Gall's Law).
When promoted, the Skill must **derive from the L3 card and stay in sync** — otherwise we get two diverging
representations (RC4 drift). The L3 card remains the SSOT.

## Evidence / learnings

- First run = self-application: the methodology decisions recorded on 2026-06-09 were produced by running
  this very practice for the first time.
