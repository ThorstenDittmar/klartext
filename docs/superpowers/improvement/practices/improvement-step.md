# Practice: Improvement Step

> **Scope.** This document defines the **Improvement Step** practice — one composable brick in our Practice
> Library.
> **Out of scope.** Other practices live in their own cards under `practices/`; method decisions and rationale
> live in `continuous-improvement.md`.
> **Anti-pattern guarded.** Advisory-only process knowledge that lives in chat and gets lost (see
> `continuous-improvement.md` §0 / RC2).
> **Language.** English — documentation-language rule.
>
> **Status:** active (ritual) · **Owner:** OE · **Advances Alpha:** Way of Working
> **Source check:** verified against the Essence standard — this is a Practice that advances the
> Way-of-Working Alpha (the cross-check itself is a four-eyes review against an external standard).

## Goal

Evolve the multi-agent system's way of working **one decision at a time**, such that **no insight is ever
lost** — without anyone having to remember to capture it. This is the load-bearing practice of our
continuous improvement loop.

## Advances Alpha

**Way of Working** — each run pushes it toward the states *In Use* → *Working Well*.

## Steps

1. **Pick up** — name exactly **one** topic/decision (not several at once).
2. **Classify** — locate the topic in Essence terms **before** discussing solutions: which element type is it
   (Alpha / Practice / Pattern / Work Product / Activity / Competency)? Which Alpha does it advance? Does the
   standard already predefine it (**KB-first lookup**: `semat-definition.md` + glossary first, web only on a
   miss)? This step keeps us *thinking* in the meta-language instead of inventing parallel concepts.
3. **Clarify & decide** — present options + a recommendation; the **user decides**.
4. **Record** — store the decision at its agreed home, **including the rationale (the "why")**.
   This is the capture-first pillar.
5. **Route** — inform affected agents via the `knowledge-routing` skill (**the user is the channel**).
   This step is what earns the Way-of-Working state *In Use* (the team actually uses the practice).
6. **Set next step** — leave a pointer to the next open topic, so the loop never stalls and a `/compact`
   cannot sever it.

## Work Products

- Decision + rationale recorded at a durable home (e.g. `continuous-improvement.md`).
- New/updated terms in `semat-glossary.md`.
- Knowledge briefings routed to affected agents (or an explicit "none affected").
- A "next step" pointer.

## Completion Checklist (Done)

- [ ] Exactly **one** topic was addressed.
- [ ] The topic was located in Essence terms (element type + Alpha) and checked against the standard
  (KB-first) **before** deciding.
- [ ] A decision was made by the user.
- [ ] Decision **and rationale** are recorded at a durable home (not only in chat).
- [ ] Affected agents identified; briefings formulated and routed — or explicitly "none affected".
- [ ] A pointer to the next step exists.

## Enforcement

Currently a **ritual** (Enforcement Hierarchy level 2). Promotion to a **Skill** (closer to mechanical) is
**deferred until after the first real run** validates the shape ("instantiate through use" — Gall's Law).
When promoted, the Skill must **derive from this card and stay in sync** — otherwise we get two diverging
representations (RC4 drift). This card remains the SSOT.

## Notes

- First run = self-application: the methodology decisions recorded on 2026-06-09 were produced by running
  this very practice for the first time.
