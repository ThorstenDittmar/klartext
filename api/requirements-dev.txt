# All dependencies (runtime + dev tools) are defined in pyproject.toml.
# This file is a thin wrapper so scripts that use `pip install -r requirements-dev.txt`
# continue to work.  To add or remove a dependency, edit pyproject.toml only.
-e .[dev]
